{
  "name": "@lab/provider-events",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "device-gateway: MQTT telemetry publisher, verified against the telemetry-processor message contract.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:pact": "vitest run test/pact",
    "test:drift": "vitest run test/demos",
    "publish:demo": "tsx src/mqtt-publisher.ts"
  },
  "dependencies": {
    "mqtt": "5.15.2"
  },
  "devDependencies": {
    "@pact-foundation/pact": "17.1.2",
    "@types/node": "22.20.1",
    "tsx": "4.23.12",
    "typescript": "5.9.3",
    "vitest": "4.1.11"
  }
}
