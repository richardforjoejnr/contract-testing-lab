{
  "name": "@lab/consumer-events",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "telemetry-processor: MQTT message consumer. Owns the message contract for device telemetry.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "subscribe": "tsx src/mqtt-subscriber.ts"
  },
  "dependencies": {
    "mqtt": "5.15.2"
  },
  "devDependencies": {
    "@pact-foundation/pact": "17.1.2",
    "@types/node": "22.20.1",
    "tsx": "4.23.12",
    "typescript": "5.9.3",
    "vitest": "4.1.11"
  }
}
