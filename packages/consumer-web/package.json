{
  "name": "@lab/consumer-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "web-dashboard: HTTP consumer of orders-api. Owns the consumer-driven contract.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@pact-foundation/pact": "17.1.2",
    "@types/node": "22.20.1",
    "typescript": "5.9.3",
    "vitest": "4.1.11"
  }
}
