node_modules/
dist/
coverage/
*.tsbuildinfo

# Pact artefacts are build output, not source. They are regenerated by the
# consumer test run and published to the broker, which is the single source of
# truth. See README "Why pacts are not committed".
pacts/
logs/
*.log

.DS_Store
.env
.env.local
.idea/
.vscode/*
!.vscode/extensions.json
